# Netlify build config — no separate CI needed. Connect the repo once and
# Netlify auto-builds and deploys on every push to the production branch.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
